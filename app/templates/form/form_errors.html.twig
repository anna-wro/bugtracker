{% block form_errors %}
    {% if errors|length > 0 -%}
        <ul class="errors-list">
            {%- for error in errors -%}
                <li class="error">{{ error.message }}</li>
            {%- endfor -%}
        </ul>
    {%- endif %}
{% endblock %}
