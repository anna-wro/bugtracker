<tr>
    <th class="icon-small-space">ID
        {% if sortBy == 'id' %}
            {% if sortOrder == 'asc' %}
                <a href="{{ url('bug_index_sorted',
                {'sortBy': 'id', 'sortOrder' : 'desc', 'status': status, 'priority': priority, 'category':category}) }}"
                   class="sort sort-asc sort--active"
                   title="{{ 'label.sort_desc'|trans }}"> </a>
            {% else %}
                <a href="{{ url('bug_index_sorted',
                {'sortBy': 'id', 'sortOrder' : 'asc', 'status': status, 'priority': priority, 'category':category}) }}"
                   class="sort sort-desc sort--active"
                   title="{{ 'label.sort_asc'|trans }}"> </a>
            {% endif %}
        {% else %}
            <a href="{{ url('bug_index_sorted',
            {'sortBy': 'id', 'sortOrder' : 'desc', 'status': status, 'priority': priority, 'category':category}) }}"
               class="sort"
               title="{{ 'label.sort_asc'|trans }}"> </a>
        {% endif %}
    </th>
    <th>{{ 'label.bug_name'|trans }}

        {% if sortBy == 'name' %}
            {% if sortOrder == 'asc' %}
                <a href="{{ url('bug_index_sorted',
                {'sortBy': 'name', 'sortOrder' : 'desc', 'status': status, 'priority': priority, 'category':category}) }}"
                   class="sort sort-asc sort--active"
                   title="{{ 'label.sort_desc'|trans }}"> </a>
            {% else %}
                <a href="{{ url('bug_index_sorted',
                {'sortBy': 'name', 'sortOrder' : 'asc', 'status': status, 'priority': priority, 'category':category}) }}"
                   class="sort sort-desc sort--active"
                   title="{{ 'label.sort_asc'|trans }}"> </a>
            {% endif %}
        {% else %}
            <a href="{{ url('bug_index_sorted',
            {'sortBy': 'name', 'sortOrder' : 'asc', 'status': status, 'priority': priority, 'category':category}) }}"
               class="sort"
               title="{{ 'label.sort_asc'|trans }}"> </a>
        {% endif %}
    </th>
    <th>{{ 'label.bug_project'|trans }}
    </th>
    <th>{{ 'label.bug_type'|trans }}

        {% if sortBy == 'type' %}
            {% if sortOrder == 'asc' %}
                <a href="{{ url('bug_index_sorted',
                {'sortBy': 'type', 'sortOrder' : 'desc', 'status': status, 'priority': priority, 'category':category}) }}"
                   class="sort sort-asc sort--active"
                   title="{{ 'label.sort_desc'|trans }}"> </a>
            {% else %}
                <a href="{{ url('bug_index_sorted',
                {'sortBy': 'type', 'sortOrder' : 'asc', 'status': status, 'priority': priority, 'category':category}) }}"
                   class="sort sort-desc sort--active"
                   title="{{ 'label.sort_asc'|trans }}"> </a>
            {% endif %}
        {% else %}
            <a href="{{ url('bug_index_sorted',
            {'sortBy': 'type', 'sortOrder' : 'asc', 'status': status, 'priority': priority, 'category':category}) }}"
               class="sort"
               title="{{ 'label.sort_asc'|trans }}"> </a>
        {% endif %}
    </th>
    <th class="icon-small-space">{{ 'label.bug_priority'|trans }}
        {% if sortBy == 'priority' %}
            {% if sortOrder == 'asc' %}
                <a href="{{ url('bug_index_sorted',
                {'sortBy': 'priority', 'sortOrder' : 'desc', 'status': status, 'priority': priority, 'category':category}) }}"
                   class="sort sort-asc sort--active"
                   title="{{ 'label.sort_desc'|trans }}"> </a>
            {% else %}
                <a href="{{ url('bug_index_sorted',
                {'sortBy': 'priority', 'sortOrder' : 'asc', 'status': status, 'priority': priority, 'category':category}) }}"
                   class="sort sort-desc sort--active"
                   title="{{ 'label.sort_asc'|trans }}"> </a>
            {% endif %}
        {% else %}
            <a href="{{ url('bug_index_sorted',
            {'sortBy': 'priority', 'sortOrder' : 'asc', 'status': status, 'priority': priority, 'category':category}) }}"
               class="sort"
               title="{{ 'label.sort_asc'|trans }}"> </a>
        {% endif %}
    </th>
    <th>{{ 'label.bug_status'|trans }}
        {% if sortBy == 'status' %}
            {% if sortOrder == 'asc' %}
                <a href="{{ url('bug_index_sorted',
                {'sortBy': 'status', 'sortOrder' : 'desc', 'status': status, 'priority': priority, 'category':category}) }}"
                   class="sort sort-asc sort--active"
                   title="{{ 'label.sort_desc'|trans }}"> </a>
            {% else %}
                <a href="{{ url('bug_index_sorted',
                {'sortBy': 'status', 'sortOrder' : 'asc', 'status': status, 'priority': priority, 'category':category}) }}"
                   class="sort sort-desc sort--active"
                   title="{{ 'label.sort_asc'|trans }}"> </a>
            {% endif %}
        {% else %}
            <a href="{{ url('bug_index_sorted',
            {'sortBy': 'status', 'sortOrder' : 'asc', 'status': status, 'priority': priority, 'category':category}) }}"
               class="sort"
               title="{{ 'label.sort_asc'|trans }}"> </a>
        {% endif %}
    </th>
    <th class="action-column"></th>
    <th class="action-column"></th>
    <th class="action-column"></th>
</tr>